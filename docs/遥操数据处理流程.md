# 遥操数据处理流程

本文档记录当前 `pika_ros` 遥操采集数据到 RobotWin 训练数据的处理流程。

示例任务：

- task name: `pnp_bread`
- instruction: `Pick up two breads, then place them onto the blue plate.`
- 原始数据目录：`/home/piper/agilex/pnp_bread`

## 0. 环境

建议使用系统 Python 跑和 OpenCV 相关的脚本：

```bash
/usr/bin/python3
```

如果使用 conda，环境至少需要：

```bash
conda create -n robotwin-data python=3.10 -y
conda activate robotwin-data
conda install -c conda-forge numpy h5py opencv scipy pyyaml -y
```

## 1. 生成视频预览

先为每个 episode 生成三视角拼接视频：

```bash
# /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
#   --task-name pnp_bread \
#   --overwrite

  # /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
  #   --task-name stack_cup \
  #   --episode-subdir unprocessed \
  #   --overwrite

  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name pick_diverse_bottles \
    --episode-subdir unprocessed \
    --overwrite


  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name place_bread_basket \
    --episode-subdir unprocessed \
    --overwrite

  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name pick_diverse_bottles \
    --episode-subdir unprocessed \
    --overwrite

  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name handover_bottle \
    --episode-subdir unprocessed \
    --overwrite


  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name pnp_bread_0512 \
    --episode-subdir unprocessed \
    --overwrite
  
  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name pnp_tray  \
    --episode-subdir unprocessed \
    --overwrite

```
### cartin data
```bash
  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name stack_cup_0609 \
    --episode-subdir unprocessed \
    --overwrite

  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name pick_diverse_bottles_0609 \
    --episode-subdir unprocessed \
    --overwrite


  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name place_bread_basket_0609  \
    --episode-subdir unprocessed \
    --overwrite

  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name pick_diverse_bottles_0609  \
    --episode-subdir unprocessed \
    --overwrite

  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name handover_bottle_0609  \
    --episode-subdir unprocessed \
    --overwrite
  
  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name pnp_tray_0609   \
    --episode-subdir unprocessed \
    --overwrite

  /usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
    --task-name pnp_bread_0609   \
    --episode-subdir unprocessed \
    --overwrite
```

每个 episode 下会生成：

```bash
camera_overview.mp4
```

## 2. 频率可视化

查看整个任务目录的采集频率：

```bash
/usr/bin/python3 ~/pika_ros/scripts/analyze_episode_hz.py \
  --task-name pnp_bread

/usr/bin/python3 ~/pika_ros/scripts/analyze_episode_hz.py \
  --task-name stack_cup 

```

查看单个 episode：

```bash
/usr/bin/python3 ~/pika_ros/scripts/analyze_episode_hz.py \
  --task-name pnp_bread \
  30
```

查看多个 episode：

```bash
/usr/bin/python3 ~/pika_ros/scripts/analyze_episode_hz.py \
  --task-name pnp_bread \
  30 31 32
```

## 3. 自动筛选

自动筛选会把原始根目录下的 `episode*` 移动到：

- `/home/piper/agilex/pnp_bread/good/episodeX`
- `/home/piper/agilex/pnp_bread/bad/episodeX`

筛选规则：

- 缺少三路 RGB 视角则进入 `bad`
- 缺少 state/action 所需的 puppet pose 或 gripper 数据则进入 `bad`
- 缺少 `camera_overview.mp4` 则进入 `bad`
- 三路 RGB 最低频率低于 `8 Hz` 则进入 `bad`
- 三路 RGB 最低频率在 `[8, 10)` 会进入 `good`，但终端黄色提示并记录日志

默认使用 robot gripper 检查：

```bash
/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pnp_bread


```

如果最终处理使用 sensor gripper，自动筛选时也建议检查 sensor gripper 是否存在：

```bash
/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pnp_bread_0609 \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name stack_cup_0609 \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pick_diverse_bottles_0609 \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name place_bread_basket_0609 \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name handover_bottle_0609 \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pnp_tray_0609 \
  --gripper-source sensor
  
```
### 3.1 cartin data
```

如果最终处理使用 sensor gripper，自动筛选时也建议检查 sensor gripper 是否存在：

```bash
/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pnp_bread \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name stack_cup \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pick_diverse_bottles \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name place_bread_basket \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name handover_bottle \
  --gripper-source sensor


/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pnp_bread_0512 \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pnp_tray \
  --gripper-source sensor
  
```

```bash
cd /home/piper/agilex

src="pnp_bread_0522/unprocessed"
dst="pnp_bread_0512/unprocessed"

start_idx=31

for ep in $(ls $src | grep '^episode[0-9]\+$' | sort -V); do
    old_idx=${ep#episode}
    new_idx=$((start_idx + old_idx))

    echo "Moving $ep -> episode$new_idx"

    mv "$src/$ep" "$dst/episode$new_idx"
done
```

日志位置：

```bash
/home/piper/agilex/pnp_bread/curation_log.jsonl
```

注意：

- 该步骤会移动目录。
- 如果已经存在 `good/episodeX` 或 `bad/episodeX`，默认不会覆盖。
- 确认要覆盖时再加 `--overwrite-existing`。

## 4. 人工视频筛选

自动筛选后，对 `good/` 里的 episode 顺序播放 `camera_overview.mp4`：

```bash
/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name pnp_bread_0609

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name stack_cup_0609

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name place_bread_basket_0609


/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name pick_diverse_bottles_0609

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name handover_bottle_0609


/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name pnp_tray_0609
```
### 4.1 cartin
```bash
/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name pnp_bread_0609

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name stack_cup_0609

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name place_bread_basket_0609


/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name pick_diverse_bottles_0609

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name handover_bottle_0609

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name pnp_tray_0609
  #第五个

```

播放窗口快捷键：

- `g`：标记为好，保留在 `good`
- `m`：标记为中等质量，移动到 `medium`
- `n`：下一个，等价于保留
- `b`：标记为坏，移动到 `bad`
- `p`：上一个
- `r`：重播当前视频
- `a`：降低播放速度
- `d`：提高播放速度
- `space`：暂停/继续
- `q`：退出人工筛选

窗口会实时显示：

- 当前是第几个 `good` 视频
- 本轮人工审核总共有多少个 `good` 视频
- 已经手动标注了多少个
- 其中有多少个被判为 `good / medium / bad`
- 当前播放速度

说明：

- `medium` 默认和 `bad` 一样，不参与后续 HDF5 处理。
- 这样可以把“明显坏数据”和“边缘质量数据”分开保留。

日志位置：

```bash
/home/piper/agilex/pnp_bread/manual_curation_log.jsonl
```

## 5. 静止帧裁剪

对人工筛选后的 `good` 数据进行静止帧裁剪，输出到新目录，不覆盖原始数据：


```bash
# /usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
#   /home/piper/agilex/pnp_bread/good \
#   --output-dir /home/piper/agilex/pnp_bread/good_trimmed_01mm \
#   --motion-threshold 0.0001 \
#   --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/stack_cup/good \
  --output-dir /home/piper/agilex/stack_cup/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/place_bread_basket/good \
  --output-dir /home/piper/agilex/place_bread_basket/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --overwrite

# 改了逻辑
/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/pick_diverse_bottles/good \
  --output-dir /home/piper/agilex/pick_diverse_bottles/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/handover_bottle/good \
  --output-dir /home/piper/agilex/handover_bottle/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/pnp_bread_0512/good \
  --output-dir /home/piper/agilex/pnp_bread_0512/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/pnp_tray/good \
  --output-dir /home/piper/agilex/pnp_tray/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite
```


### cartin删除静止帧
```bash
/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/pnp_bread_0609/good \
  --output-dir /home/piper/agilex/pnp_bread_0609/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/stack_cup_0609/good \
  --output-dir /home/piper/agilex/stack_cup_0609/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/place_bread_basket_0609/good \
  --output-dir /home/piper/agilex/place_bread_basket_0609/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --overwrite

# 改了逻辑
/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/pick_diverse_bottles_0609/good \
  --output-dir /home/piper/agilex/pick_diverse_bottles_0609/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/handover_bottle_0609/good \
  --output-dir /home/piper/agilex/handover_bottle_0609/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite

/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/pnp_tray_0609/good \
  --output-dir /home/piper/agilex/pnp_tray_0609/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --overwrite
```

如果你想单独检查某个 `episode` 为什么有帧被删除，可以直接看“被删帧”和“上一保留帧 / 上一原始帧”的 7 维差值：

```bash
/usr/bin/python3 ~/pika_ros/scripts/inspect_trim_static_episode.py \
  /home/piper/agilex/pnp_bread/good/episode147 \
  --motion-threshold 0.0001 \
  --gripper-source sensor \
  --limit 30 \
  --output-json /home/piper/agilex/pnp_bread/debug_trim/episode147_inspection.json

/usr/bin/python3 ~/pika_ros/scripts/inspect_trim_static_episode.py \
  /home/piper/agilex/place_bread_basket/good/episode10 \
  --motion-threshold 0.0001 \
  --limit 30 \
  --output-json /home/piper/agilex/place_bread_basket/debug_trim_episode10_inspection.json
```

这个检查脚本会输出：

- 每个被删帧相对“上一保留帧”的左右手 7 维绝对变化量之和
- 每个被删帧相对“上一原始帧”的左右手 7 维绝对变化量之和
- 每一维的详细差值：
  - `x, y, z, roll, pitch, yaw, gripper_distance`

这样你可以区分：

- 机器人确实几乎没动
- 机器人在连续微动，但累计到上一保留帧的变化仍然低于阈值
- 某个维度的单位或权重让阈值判断不符合直觉

规则：

- 基于头部相机时间线对齐数据
- `--gripper-source robot` 时：
  - 每只手使用 7 维：
    - `x, y, z, roll, pitch, yaw, robot_gripper_distance`
- `--gripper-source sensor` 时：
  - 每只手使用 8 维：
    - `x, y, z, roll, pitch, yaw, robot_gripper_distance, sensor_gripper_distance`
  - 也就是不会用 sensor 替换 robot gripper，而是二者都参与“是否静止”的判断
- 左右手分别计算对应维度绝对变化量之和
- 如果某一帧相对上一个保留帧的左右手最大“对应维度绝对变化量之和”小于阈值，则删除
- 第一帧和最后一帧始终保留

关于旋转量：

- `roll, pitch, yaw` 直接读取原始 `endPose` JSON 中的值
- 单位是 `rad`
- 当前脚本不会把旋转量换算成米，也不会做额外归一化
- 也就是说，当前位置变化 `m`、旋转变化 `rad`、robot gripper 变化量、sensor gripper 变化量会直接一起参与上面的求和阈值
- 因此 `--motion-threshold` 目前是一个工程上的混合阈值，不是严格物理同量纲的距离
- 所以 `--motion-threshold 0.0001` 不能直接理解成“严格的 0.1 mm 阈值”
- 只有在旋转项和夹爪项几乎不变时，它才大致接近“位置总变化量约 0.1 mm”
- 如果后面你觉得旋转量权重过大或过小，再单独给旋转项加系数会更稳

  如果只有位置变化：

  - 阈值是 0.0001
  - 位置单位是 m
  - 所以单独一维大约要到 0.0001 m
  - 也就是 0.1 mm

  如果只有朝向变化：

  - roll/pitch/yaw 单位是 rad
  - 单独一维要到 0.0001 rad
  - 换算成角度：
      - 0.0001 * 180 / pi ≈ 0.00573°
  - 也就是大约 0.0057 度
为什么看起来“视频不卡”，但仍然会删掉较多帧：

- 这个规则不是按视觉卡顿判断，而是按左右手状态量变化是否足够大判断
- 如果机器人在连续微动，但相邻若干帧的累计状态变化始终低于阈值，这些帧仍然会被删
- 尤其在 10 Hz 采集下，很多慢速接近、微调、夹持保持阶段，本来就会出现较高比例的“近静止帧”
- 如果你想确认是否删得过头，建议先用 `inspect_trim_static_episode.py` 检查一个具体 episode 的被删帧明细

裁剪输出目录中的 `camera_overview.mp4`：

- 现在不会再直接复制原始 episode 里的 `camera_overview.mp4`
- 脚本会根据裁剪后保留下来的图片重新生成新的 `camera_overview.mp4`
- 所以 `good_trimmed_*` 目录里的总览视频已经对应“删帧之后”的结果

输出报告：

```bash
/home/piper/agilex/pnp_bread/good_trimmed_01mm/static_trim_report.json
```

报告中包含每个 episode：

- 原始帧数
- 剩余帧数
- 删除帧数
- 剩余百分比
- 输出目录

## 6. 转换为 RobotWin HDF5

处理人工筛选后的 good 数据：

Stack the dark red and light red cups onto the green cup.

```bash
/usr/bin/python3 ~/pika_ros/scripts/process_data_robotwin_headcam.py \
  /home/piper/agilex/pnp_bread \
  "Pick up two breads, then place them onto the blue plate." \
  147 \
  --episode-subdir good \
  --gripper-source sensor \
  --output-dir /home/piper/agilex/processed_robotwin/pnp_bread-good-147-sensor
```

处理静止帧裁剪后的数据：

```bash
/usr/bin/python3 ~/pika_ros/scripts/process_data_robotwin_headcam.py \
  /home/piper/agilex/pnp_bread \
  "Pick up two breads, then place them onto the blue plate." \
  147 \
  --episode-subdir good_trimmed_01mm \
  --gripper-source sensor \
  --output-dir /home/piper/agilex/processed_robotwin/pnp_bread-good-trimmed-01mm-147-sensor

  python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
    /home/piper/agilex/stack_cup \
    "Stack the dark red and light red cups onto the green cup." \
    81 \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-dir /home/piper/agilex/processed_robotwin/stack_cups-good-trimmed-01mm-81-sensor

# Use one arm to pick up the bread, put it into the basket, and use another arm to lift the basket
  python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
    /home/piper/agilex/place_bread_basket \
    "Use one arm to pick up the bread, put it into the basket, and use another arm to lift the basket." \
    121 \
    --episode-subdir good_trimmed_01mm \
    --output-dir /home/piper/agilex/processed_robotwin/place_bread_basket-good-01mm-69


# pick up one bottle with one arm, and pick up another bottle with the other arm
  python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
    /home/piper/agilex/pick_diverse_bottles \
    "pick up one bottle with one arm, and pick up another bottle with the other arm." \
    95 \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-dir /home/piper/agilex/processed_robotwin/pick_diverse_bottles-good-01mm-83


# Use the left arm to grasp the red block on the table, handover it to the right arm and place it on the blue pad.
  python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
    /home/piper/agilex/handover_bottle \
    "Use the right arm to grasp the bottle on the table, handover it to the left arm." \
    66 \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-dir /home/piper/agilex/processed_robotwin/handover_bottle-good-01mm-66

python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
  /home/piper/agilex/pnp_bread_0512 \
  "Pick up two breads, then place them onto the blue plate." \
  47 \
  --episode-subdir good_trimmed_01mm \
  --gripper-source sensor \
  --output-dir /home/piper/agilex/processed_robotwin/pnp_bread-good-trimmed-01mm-47-sensor

python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
  /home/piper/agilex/pnp_tray \
  "Use the left arm to grasp the red cup, and use the right arm to grasp the bottle, then place them onto the blue tray." \
  45 \
  --episode-subdir good_trimmed_01mm \
  --gripper-source sensor \
  --output-dir /home/piper/agilex/processed_robotwin/pnp_tray-good-trimmed-01mm-45-sensor

```

说明：

- `--episode-subdir good` 表示从 `/home/piper/agilex/pnp_bread/good/episode*` 读取。
- `--episode-subdir good_trimmed_01mm` 表示从裁剪后的目录读取。
- `--gripper-source sensor` 表示 state/action 中的 gripper 使用 `pikaSensor_l/r`。
### cartin

```bash
python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
  /home/piper/agilex/pnp_bread_0609 \
  "Pick up two breads, then place them onto the blue plate." \
  11 \
  --episode-subdir good_trimmed_01mm \
  --gripper-source sensor \
  --output-dir /home/piper/agilex/processed_robotwin/pnp_bread-good-trimmed-01mm-11-sensor

  python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
    /home/piper/agilex/stack_cup_0609  \
    "Stack the dark red and light red cups onto the green cup." \
    18 \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-dir /home/piper/agilex/processed_robotwin/stack_cups-good-trimmed-01mm-18-sensor

# Use one arm to pick up the bread, put it into the basket, and use another arm to lift the basket
  python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
    /home/piper/agilex/place_bread_basket_0609 \
    "Use one arm to pick up the bread, put it into the basket, and use another arm to lift the basket." \
    11 \
    --episode-subdir good_trimmed_01mm \
    --output-dir /home/piper/agilex/processed_robotwin/place_bread_basket-good-01mm-11


# pick up one bottle with one arm, and pick up another bottle with the other arm
  python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
    /home/piper/agilex/pick_diverse_bottles_0609 \
    "pick up one bottle with one arm, and pick up another bottle with the other arm." \
    10 \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-dir /home/piper/agilex/processed_robotwin/pick_diverse_bottles-good-01mm-10


# Use the left arm to grasp the red block on the table, handover it to the right arm and place it on the blue pad.
  python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
    /home/piper/agilex/handover_bottle_0609 \
    "Use the right arm to grasp the bottle on the table, handover it to the left arm." \
    17 \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-dir /home/piper/agilex/processed_robotwin/handover_bottle-good-01mm-17

python ~/pika_ros/scripts/process_data_robotwin_headcam.py \
  /home/piper/agilex/pnp_tray_0609 \
  "Use the left arm to grasp the red cup, and use the right arm to grasp the bottle, then place them onto the blue tray." \
  13 \
  --episode-subdir good_trimmed_01mm \
  --gripper-source sensor \
  --output-dir /home/piper/agilex/processed_robotwin/pnp_tray-good-trimmed-01mm-13-sensor

```

## 7. 检查处理后的 HDF5

检查一个处理后的 episode：

```bash
# /usr/bin/python3 ~/pika_ros/scripts/check_processed_robotwin_headcam.py \
#   /home/piper/agilex/processed_robotwin/pnp_bread-good-trimmed-1mm-147-sensor \
#   --episode 0

# python ~/pika_ros/scripts/check_processed_robotwin_headcam.py \
#   /home/piper/agilex/processed_robotwin/stack_cups-good-trimmed-01mm-60-sensor\
#   --episode 0

python ~/pika_ros/scripts/check_processed_robotwin_headcam.py \
  /home/piper/agilex/processed_robotwin/place_bread_basket-good-01mm-69\
  --episode 0
```

导出图片预览：

```bash
/usr/bin/python3 ~/pika_ros/scripts/export_robotwin_hdf5_preview_images.py \
  /home/piper/agilex/processed_robotwin/pnp_bread-good-trimmed-1mm-147-sensor/episode_0/episode_0.hdf5
```

默认预览输出：

```bash
/home/piper/agilex/robotwin_hdf5_previews/pnp_bread-good-trimmed-1mm-147-sensor/episode_0
```

是的，这条命令用的是 sensor 数据。关键就在这里：

  --gripper-source sensor

  这会让 [process_data_robotwin_headcam.py](/home/piper/pika_ros/scripts/
  process_data_robotwin_headcam.py) 读取：

  - gripper/encoder/pikaSensor_l
  - gripper/encoder/pikaSensor_r

  而不是：

  - gripper/encoder/pikaGripper_l
  - gripper/encoder/pikaGripper_r

  我给你补了一个统计脚本：pika_ros/scripts/summarize_teleop_state_ranges.py

  你可以直接看 good_trimmed_01mm 里每个 episode、每个维度的取值范围：

```bash
  python ~/pika_ros/scripts/summarize_teleop_state_ranges.py \
    /home/piper/agilex/pnp_bread \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-json /home/piper/agilex/pnp_bread/debug_ranges/good_trimmed_01mm_sensor_ranges.json

  python ~/pika_ros/scripts/summarize_teleop_state_ranges.py \
    /home/piper/agilex/stack_cup \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-json /home/piper/agilex/stack_cup/debug_ranges/good_trimmed_01mm_sensor_ranges.json

  python ~/pika_ros/scripts/summarize_teleop_state_ranges.py \
    /home/piper/agilex/place_bread_basket \
    --episode-subdir good_trimmed_01mm \
    --output-json /home/piper/agilex/place_bread_basket/debug_ranges_good_trimmed_01mm_ranges.json

  python ~/pika_ros/scripts/summarize_teleop_state_ranges.py \
    /home/piper/agilex/pick_diverse_bottles \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --output-json /home/piper/agilex/pick_diverse_bottles/debug_ranges/good_trimmed_01mm_sensor_ranges.json
```
  它会输出每个 episode 的 14 维范围：

  - left_x/y/z/roll/pitch/yaw/gripper
  - right_x/y/z/roll/pitch/yaw/gripper

  如果你只想先快速看左右 gripper 的范围，用精简模式：
  python ~/pika_ros/scripts/summarize_teleop_state_ranges.py \
    /home/piper/agilex/pnp_bread \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --compact

  如果你只看几个 episode，比如 147 和 149：
  python ~/pika_ros/scripts/summarize_teleop_state_ranges.py \
    /home/piper/agilex/pnp_bread

  python ~/pika_ros/scripts/summarize_teleop_state_ranges.py \
    /home/piper/agilex/handover_bottle \
    --episode-subdir good_trimmed_01mm \
    --gripper-source sensor \
    --compact

## 8.传输文件
```bash
zip -r  pnp_bread-01mm-147-sensor0511.zip /home/piper/agilex/processed_robotwin/pnp_bread-good-trimmed-01mm-147-sensor
rclone copy  /home/piper/agilex/processed_robotwin/pnp_bread-01mm-147-sensor0511.zip gdrive_yzj:piper/pnp_bread-01mm-147-sensor0511/ -P --dry-run


zip -r  stack_cups-60-sensor.zip /home/piper/agilex/processed_robotwin/stack_cups-good-trimmed-01mm-60-sensor
rclone copy  /home/piper/agilex/processed_robotwin/stack_cups-60-sensor.zip gdrive_yzj:piper/stack_cups-60-sensor0512/ -P --dry-run

zip -r place_bread_basket-69.zip place_bread_basket-good-01mm-69/
rclone copy  /home/piper/agilex/processed_robotwin/place_bread_basket-69.zip gdrive_yzj:piper/processed/place_bread_basket-69/ -P --dry-run


zip -r pick_diverse_bottles-83.zip ./pick_diverse_bottles-good-01mm-83
rclone copy  /home/piper/agilex/processed_robotwin/pick_diverse_bottles-83.zip  gdrive_yzj:piper/processed/pick_diverse_bottles-83/ -P --dry-run

zip -r handover_bottle-66.zip /home/piper/agilex/processed_robotwin/handover_bottle-good-01mm-66
rclone copy  /home/piper/agilex/processed_robotwin/handover_bottle-66.zip  gdrive_yzj:piper/processed/handover_bottle-66/ -P --dry-run

zip -r pnp_bread-47-sensor0525.zip /home/piper/agilex/processed_robotwin/pnp_bread-good-trimmed-01mm-47-sensor
rclone copy  /home/piper/agilex/processed_robotwin/pnp_bread-47-sensor0525.zip  gdrive_yzj:piper/processed/pnp_bread-47-sensor0525/ -P --dry-run

zip -r pnp_tray-45.zip /home/piper/agilex/processed_robotwin/pnp_tray-good-trimmed-01mm-45-sensor
rclone copy  /home/piper/agilex/processed_robotwin/pnp_tray-45.zip  gdrive_yzj:piper/processed/pnp_tray-45/ -P --dry-run


zip -r robotwin_6tasks_cartin.zip \
    handover_bottle-good-01mm-17 \
    place_bread_basket-good-01mm-11 \
    pick_diverse_bottles-good-01mm-10 \
    pnp_bread-good-trimmed-01mm-11-sensor \
    pnp_tray-good-trimmed-01mm-13-sensor \
    stack_cups-good-trimmed-01mm-18-sensor
rclone copy  /home/piper/agilex/processed_robotwin/robotwin_6tasks_cartin.zip  gdrive_yzj:piper/processed/cartin_6task/ -P --dry-run
```


# 清理空间
```bash
zip -r pnp_bread_bad.zip  bad/
rclone copy  /home/piper/agilex/pnp_bread/pnp_bread_bad.zip  gdrive_yzj:piper/pnp_bread/bad/ -P --dry-run

zip -r pnp_bread_old.zip pnp_bread/
rclone copy  /home/piper/agilex/pnp_bread/pnp_bread_old.zip  gdrive_yzj:piper/pnp_bread/old_quick/ -P --dry-run


zip -r stack_cup_bad.zip  bad/
rclone copy  /home/piper/agilex/stack_cup/stack_cup_bad.zip  gdrive_yzj:piper/stack_cup/bad/ -P --dry-run

# 机器原因整体缺少右手
zip -r pick_diverse_bottles-wo-rightwrist.zip pick_diverse_bottles
rclone copy  /home/piper/agilex/pick_diverse_bottles-wo-rightwrist.zip  gdrive_yzj:piper/pick_diverse_bottles/bad/ -P --dry-run


rclone copy  /home/piper/agilex/old-table-piper.zip  gdrive_yzj:piper/old_table/ -P --dry-run

zip -r place_bread_baset_bad.zip bad
rclone copy  /home/piper/agilex/place_bread_basket/place_bread_baset_bad.zip  gdrive_yzj:piper/place_bread_basket/bad/ -P --dry-run

zip -r pnp_bread_old.zip pnp_bread/ 
rclone copy  /home/piper/agilex/pnp_bread_old.zip  gdrive_yzj:piper/pnp_bread/old/ -P --dry-run


zip -r stack_cup_all.zip stack_cup
rclone copy  /home/piper/agilex/stack_cup_all.zip   gdrive_yzj:piper/stack_cup/good/ -P --dry-run

zip -r pnp_tray_all.zip pnp_tray
rclone copy  /home/piper/agilex/pnp_tray_all.zip   gdrive_yzj:piper/pnp_tray/good/ -P --dry-run

zip -r  pnp_bread_0512_all.zip  pnp_bread_0512
rclone copy  /home/piper/agilex/pnp_bread_0512_all.zip   gdrive_yzj:piper/pnp_bread/good/ -P --dry-run

zip -r  place_bread_basket_all.zip  place_bread_basket
rclone copy  /home/piper/agilex/place_bread_basket_all.zip   gdrive_yzj:piper/place_bread_basket/all/ -P --dry-run


```


# 数据下载
```bash
rclone copy gdrive_yzj:piper/pi0_checkpoints/nsccp/pi05_zaijia_0513piper-147/place_bread_basket-69/29999.tar.gz   /home/piper/yzj/ckpt/place_bread_basket/29999 -P

rclone copy gdrive_yzj:piper/pi0_checkpoints/nsccp/pi05_zaijia_0513piper-147/pick_diverse_bottles-83/29999.tar.gz   /home/piper/yzj/ckpt/pick_diverse_bottles/29999 -P

rclone copy gdrive_yzj:piper/pi0_checkpoints/nsccp/pi05_zaijia_0513piper-147/stack_cups-60/29999.tar.gz   /home/piper/yzj/ckpt/stack_cups/29999 -P


rclone copy gdrive_yzj:piper/pi0_checkpoints/nsccp/pi05_zaijia_0513piper-147/multi_piper_basket_bottle_stack_210-1gpu/29999.tar.gz   /home/piper/yzj/ckpt/multi/29999 -P

# pi05
rclone copy gdrive_yzj:piper/pi0_checkpoints/nsccp/pi05_zaijia_0513piper-147/multi_piper_5tasks_cap50_242-1gpu/29999.tar.gz   /home/piper/yzj/ckpt/multi/29999 -P

# human
rclone copy gdrive_yzj:piper/pi0_checkpoints/nsccp/pi05_zaijia_0513piper-147/multi_piper_real25_human25_plus2_242-1gpu/29999.tar.gz   /home/piper/yzj/ckpt/multi/29999 -P


tar -tzvf 29999/29999.tar.gz | head
tar -xzvf 29999/29999.tar.gz
```

# 人手数据
```bash
zip -r human_stack_cups.zip human/stack_cups
rclone copy  /home/piper/agilex/human_stack_cups.zip  gdrive_yzj:piper/human/stack_cups/ -P --dry-run


zip -r human_pnp_star_pear.zip human/pnp_star_pear
rclone copy  /home/piper/agilex/human_pnp_star_pear.zip  gdrive_yzj:piper/human/old_table/pnp_star_pear/ -P --dry-run

zip -r human_place_bread_basket.zip human/place_bread_basket
rclone copy  /home/piper/agilex/human_place_bread_basket.zip  gdrive_yzj:piper/human/place_bread_basket/ -P --dry-run

zip -r pnp_bread-7.zip 

zip -r pnp_bread-7.zip  human/pnp_bread
rclone copy  /home/piper/agilex/pnp_bread-7.zip  gdrive_yzj:piper/human/pnp_bread/ -P --dry-run



zip -r human_handover_bottle.zip human/handover_bottle
rclone copy  /home/piper/agilex/human_handover_bottle.zip  gdrive_yzj:piper/human/handover_bottle/ -P --dry-run

zip -r human_pnp_bread.zip human/pnp_bread
rclone copy  /home/piper/agilex/human_pnp_bread.zip  gdrive_yzj:piper/human/pnp_bread/ -P --dry-run

zip -r human_pnp_tray.zip human/pnp_tray
rclone copy  /home/piper/agilex/human_pnp_tray.zip  gdrive_yzj:piper/human/pnp_tray/ -P --dry-run
```

## 9. `pnp_0825`（2026-08-25 新采集双瓶任务）

任务信息：

- 原始数据目录：`/home/piper/agilex/pnp_0825/unprocessed`
- 原始 episode 数：60（`episode0` 到 `episode59`）
- instruction：`pick up one bottle with one arm, and pick up another bottle with the other arm.`
- state/action 使用 sensor gripper

### 9.1 tmux 自动预处理

自动步骤包括生成预览视频和 sensor gripper 自动筛选。运行脚本带安全门控：重新识别出的 35 条候选数据必须全部生成非空 `camera_overview.mp4`，否则不会执行会移动目录的 `auto-split`。

```bash
tmux new-session -d -s pnp_0825_preprocess \
  "/usr/bin/bash /home/piper/agilex/pnp_0825/pnp_0825_preprocess.sh >> /home/piper/agilex/pnp_0825/pnp_0825_preprocess.log 2>&1"
```

查看会话和日志：

```bash
tmux attach -t pnp_0825_preprocess

tail -f /home/piper/agilex/pnp_0825/pnp_0825_preprocess.log
```

脚本内部执行的核心命令：

```bash
/usr/bin/python3 ~/pika_ros/scripts/render_all_episode_videos.py \
  --task-name pnp_0825 \
  --episode-subdir unprocessed

/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py auto-split \
  --task-name pnp_0825 \
  --gripper-source sensor
```

已知硬性坏数据：

- `episode0` 到 `episode22`：左腕 RGB 为 0 帧
- `episode33`：三路 RGB 都只有 1 帧
- `episode59`：三路 RGB 为 0 帧，并缺少右侧 sensor gripper 和左臂 puppet pose

按现有规则预计自动筛选得到 `good=35`、`bad/system=25`。`episode42` 的头部相机约为 `9.987 Hz`，低于目标 10 Hz 但高于最低 8 Hz，会保留在 `good` 并记录 WARNING。

### 9.2 人工视频筛选

必须等 tmux 自动预处理完成后再运行：

```bash
/usr/bin/python3 ~/pika_ros/scripts/curate_teleop_episodes.py manual-review \
  --task-name pnp_0825
```

常用快捷键：`g` 保留为 good，`m` 移入 medium，`b` 移入 bad/manual，`n` 保留并看下一条，`p` 返回上一条，`r` 重播，`space` 暂停/继续，`q` 退出。

### 9.3 人工筛选完成后的静止帧裁剪

不要在人工筛选完成前执行。该命令不覆盖 `good` 原数据：

```bash
/usr/bin/python3 ~/pika_ros/scripts/trim_static_teleop_frames.py \
  /home/piper/agilex/pnp_0825/good \
  --output-dir /home/piper/agilex/pnp_0825/good_trimmed_01mm \
  --motion-threshold 0.0001 \
  --gripper-source sensor
```

### 9.4 转换为 RobotWin HDF5

静止帧裁剪完成后执行。`35` 是最多处理条数；实际输出数由人工筛选后 `good_trimmed_01mm` 中的有效 episode 数决定。

```bash
/usr/bin/python3 ~/pika_ros/scripts/process_data_robotwin_headcam.py \
  /home/piper/agilex/pnp_0825 \
  "pick up one bottle with one arm, and pick up another bottle with the other arm." \
  35 \
  --episode-subdir good_trimmed_01mm \
  --gripper-source sensor \
  --output-dir /home/piper/agilex/processed_robotwin/pnp_0825-good-trimmed-01mm-sensor
```

检查转换后的第一条数据：

```bash
/usr/bin/python3 ~/pika_ros/scripts/check_processed_robotwin_headcam.py \
  /home/piper/agilex/processed_robotwin/pnp_0825-good-trimmed-01mm-sensor \
  --episode 0
```
